export {
  buildSectionTimeline,
  buildTimeline,
  buildVisibleActionRangeSelection,
  buildVisibleActionRows,
  deriveAnchorMs,
  findActiveIndex,
  formatOffset,
  getContiguousMergeableActionIds
} from "@jittle-lamp/shared";
export type { TimelineItem, TimelineKind, TimelineSection, VisibleActionRow } from "@jittle-lamp/shared";
