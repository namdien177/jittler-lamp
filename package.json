{
  "name": "jittle-lamp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:shared && bun run build:extension && bun run build:desktop",
    "build:shared": "bun run --cwd packages/shared build",
    "build:extension": "bun run --cwd apps/extension build",
    "build:desktop": "bun run --cwd apps/desktop build",
    "typecheck": "bun x tsc --noEmit -p tsconfig.typecheck.json",
    "test": "bun test",
    "clean": "rm -rf apps/desktop/dist apps/extension/dist packages/shared/dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.325",
    "bun-types": "latest",
    "typescript": "^5.8.3"
  }
}
