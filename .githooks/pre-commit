#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if ! git diff --cached --name-only --diff-filter=ACMR | grep -q '^apps/backend/'; then
	exit 0
fi

printf '%s\n' 'Running backend Biome check...'
bun run --cwd apps/backend lint
